from collections import Counter

k = int(input())
room_no_list = list(map(int, input().split()))

counts = Counter(room_no_list)

captains_room = [item for item, count in counts.items() if count == 1]

print(captains_room[0])
    
